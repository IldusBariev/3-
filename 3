#include <stdio.h>

int main()
{
    int numbers[3];
    
    // Заполнение массива
    printf("Введите 3 числа\n");
    for (int i = 0; i < 3; i++) {
        scanf("%d", &numbers[i]);
    }
    
    for (int i = 0; i < 3; i++) {
        printf("\n%d", numbers[i]);
    }
    
    return 0;
}
